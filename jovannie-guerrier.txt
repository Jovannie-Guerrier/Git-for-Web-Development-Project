    Completed Codepen: https://codepen.io/CyberSecDime/pen/yLaGzqo


    1. What is Semantic HTML? Semantic HTML or semantic markup is HTML that introduces meaning to the web page rather than just presentation.
    2. What is HTML used for? HTML, the Hypertext Markup Language, is a standard for describing the structure and presentation of information via the Internet
    3. What is an attribute and where do we put it? Attributes define additional characteristics or properties of the element such as width and height of an image
    4. What is the h1 tag used for? How many times should I use it on a page? The h1 tag is used as a sizing measure for headers. There is h1-h6, with h1 being the largest and scales down to h6.
    5. Name two tags that have required attributes: <div> + <img src>
    6. What do we put in the head of our HTML document? <!DOCTYPE html>
    7. What is an id? The id specifies a unique id for an HTML element.
    8. What elements can I add an id to? h1 + h2
    9. How many times can I use the same id on a page? Only once
    10. What is a class?  attribute used to specify a class for an HTML element.
    11. What elements can I add a class to? HMTL elements
    12. How many times can I use the same class on a page? Multiple times
    13. How do I get my link to open in a new tab? by adding the target=”_blank” attribute
    14. What is the alt attribute in the image tag used for? specifies an alternate text for an image, if the image cannot be displayed
    15. How do I reference an id? utilize a hastag (#)
    16. What is the difference between a section and a div?  section tag defines sections in a document like chapters, etc whereas div tag defines a division in an HTML document
    17. What is CSS used for? CSS is the language for describing the presentation of Web pages, including colors, layout, and fonts.
    18. How to we select an element? Example - every h2 on the page. You can write a hash (#) character, followed by the id of the element.
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. The difference between an ID and a class is that an ID can be used to identify one element, whereas a class can be used to identify more than one
    20. How do we select classes in CSS? With a (.) period
    21. How do we select a p element with a single class of “human”? p .human{}
    22. What is a parent child selector? When would this be useful? selects all elements that are a direct child of the specified element.
    23. How do you select all links within a div with the class of sidebar? with the assigned class name and a (.) period
    24. What is a pseudo selector? keyword added to a selector that specifies a special state of the selected element.
    25. What do we use the change the spacing between lines? Use the line-height property in CSS 
    26. What do we use to change the spacing between letters? Use the letter-spacing property.
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    28. How do I add a 1px border around my div that is dotted and black? <div style="border: dotted 1px black" id="mydiv">content</div> 
    29. How do I select everything on the page? 
    30. How do I write a comment in CSS? */ text goes here */
    31. How do I find out what file I am in, when I am using the command line? $pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? $ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? $rm
    34. Why should I use version control? 
    35. How often should I commit to github? You commit to git once you have completed all your changes.
    36. What is the command we would use to push our repo up to github? git push origin -u firstname-lastname
    37. Walk me through Lambda's git flow. 1. Fork the repo on git. 2. Clone it 3. Open up your CLI 
        4. Use the git clone command to clone the repo to the directory you are currently in. 5. cd into the newly created repo using the cd command